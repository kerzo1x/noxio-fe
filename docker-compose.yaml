services:
  frontend:
    build: 
      context: ./
    container_name: noxio-dev-app
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules        
    environment:
      - CHOKIDAR_USEPOLLING=true