services:
  frontend:
    build: 
      context: ./frontend
    container_name: noxio-frontend-dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules        
    environment:
      - CHOKIDAR_USEPOLLING=true 